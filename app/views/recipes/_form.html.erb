<%= form_for(@recipe) do |f| %>
  <% if @recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% @recipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Name *" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Source *" %><br />
    <%= f.text_field :source %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div> 

  <!= I'm not really sure where the following line should go, or if I need it. %>
  <input type="hidden" name="ingredients[]" value="json_encoded_fields" />

  <!= Should I NOT be using :ingredients here? Just random fields which will be stored as data later? => 
  <%= f.fields_for :ingredients do |fields| %>
    <div class="field">
      <%= fields.label :amount %><br />
      <%= fields.text_field :amount %>
      <%= fields.label :name %><br />
      <%= fields.text_field :name %>
    </div>
    <div class="actions">
      <%= link_to "[+]", @recipe.add_ingredient %>
      <!= Here: I want to add a new line of the form for a new ingredient and store this one in the ingredients array - json encoded? =>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label "Instructions *" %><br />
    <%= f.text_area :instructions %>
  </div>
  <div class="actions">
    <%= f.submit %>
    <!= I want to @recipe.ingredients.create each element of ingredients[] on submit =>
  </div>
<% end %>
